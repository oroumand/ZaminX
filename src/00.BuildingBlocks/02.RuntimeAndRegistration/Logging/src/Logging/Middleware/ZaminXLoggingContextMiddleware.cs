namespace ZaminX.BuildingBlocks.RuntimeAndRegistration.Logging.Middleware;

// Intentionally unused. Context enrichment is implemented via inline middleware in
// ZaminXLoggingWebApplicationExtensions to avoid activation issues when passing
// per-pipeline registrations.
internal static class ZaminXLoggingContextMiddleware
{
}
